# תשובות

**1. איך אפשר לתת ערכי ברירת מחדל ל-props של קומפוננטת פונקציה? האם נבצע זאת תמיד? מדוע?**

אפשר לתת ערך ברירת מחדל ישירות בפירוק (destructuring) של הפרופס, כך שאם לא מעבירים ערך ל-prop הזה בעת השימוש בקומפוננטה, הוא יקבל את הערך שהגדרנו.

לא נעשה את זה תמיד - רק כשהגיוני שיהיה ערך "ברירת מחדל" סביר, כלומר כשה-prop הוא לא חובה ואפשר לתפקד בלעדיו. אם ה-prop הוא קריטי לתפקוד הקומפוננטה (למשל בלי זה אין מה להציג בכלל) עדיף לא לתת ברירת מחדל אלא לוודא שהוא תמיד יועבר, אחרת הקומפוננטה עלולה "לעבוד" בלי שנשים לב שחסר לה משהו.

**2. מהו JSX?**

JSX זו הרחבה של תחביר JavaScript שמאפשרת לכתוב קוד שנראה כמו HTML בתוך קובץ JS. זה לא HTML אמיתי - זה בעצם "syntactic sugar", וכשהקוד עובר קומפילציה (על ידי Babel) הוא הופך לקריאות רגילות של `React.createElement`.

**3. מה צריך להיות בתחביר camelCase בריאקט? (3)**

- שמות אירועים כמו `onClick`, `onChange`, `onSubmit`
- תכונות סגנון (style) כמו `backgroundColor`, `fontSize`
- שמות תכונות HTML שבמקור כתובות עם מקף, למשל `tabIndex` (במקום tabindex)

**4. מה משותף בין התכונות class, for ב-HTML? האם ניתן להשתמש בהם ב-JSX?**

שתיהן מילים שמורות בשפת JavaScript (class משמשת להגדרת מחלקה, for משמש ללולאה). בגלל זה אי אפשר להשתמש בהן כמו שהן בתור שם attribute ב-JSX - צריך להשתמש בשם חלופי (className, htmlFor).

**5. מה זה expression js והיכן נשתמש בו ב-JSX?**

expression הוא כל דבר בקוד שמחזיר ערך - יכול להיות משתנה, חישוב מתמטי, קריאה לפונקציה, תנאי עם טרנרי וכו'. ב-JSX נשתמש ב-expression בכל מקום שבו נרצה להכניס ערך דינמי מתוך JS, על ידי עטיפה שלו בסוגריים מסולסלים `{ }`.

**6. איך ריאקט מזהה האם התגית היא תגית רגילה ב-HTML או קומפוננטת ריאקט?**

לפי האות הראשונה של שם התגית - אם היא מתחילה באות קטנה ריאקט מתייחס אליה כתגית HTML רגילה, ואם היא מתחילה באות גדולה ריאקט מתייחס אליה כקומפוננטה שהוגדרה על ידינו.

**7. מה ההבדל בין הערך שניתן להכניס בתוך {} שהוא תוכן של תגית, לערך כאשר מדובר על תוכן של תכונה?**

כשה-`{}` הוא תוכן של תגית (בין ה-tags), הערך שבפנים בעצם מתרנדר לתוך העמוד - זה יכול להיות מחרוזת, מספר, מערך של אלמנטים וכו', וריאקט "מציג" אותו כתוכן.
כשה-`{}` הוא בתוך תכונה (attribute), הערך צריך להתאים לסוג שהתכונה מצפה לו (מחרוזת, בוליאני, פונקציה וכו') והוא לא מוצג כטקסט על המסך אלא רק קובע את ההתנהגות/מראה של האלמנט.

**8. מה נעשה כאשר נרצה להחזיר מספר תגיות מתוך קומפוננטה? (2 אפשרויות)**

1. לעטוף את כל התגיות בתגית הורה אחת.
2. להשתמש ב-Fragment כדי לעטוף אותן בלי להוסיף אלמנט נוסף בפועל ל-DOM.

**9. מהי התגית השקופה Fragment? מהם הדרכים לכתוב אותה? (3) האם ניתן לשים עליה תכונות?**

Fragment היא תגית "שקופה" שמאפשרת לקבץ כמה אלמנטים תחת הורה אחד בלי להוסיף אלמנט נוסף בפועל ל-DOM (בניגוד ל-div שכן נוסף בפועל).

דרכים לכתוב אותה:
1. `<React.Fragment> ... </React.Fragment>`
2. `<Fragment> ... </Fragment>` (אחרי import של Fragment מ-react)
3. הצורה המקוצרת: `<> ... </>`

ניתן לשים עליה תכונות (כמו key) רק כשכותבים אותה בצורה המלאה (`React.Fragment` או `Fragment`). בצורה המקוצרת `<></>` אי אפשר להוסיף שום תכונה.
